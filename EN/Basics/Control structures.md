```cpp
	if (bool) statement;
	while (bool) statement;
	do statement; while(bool);
	for(declaration||expression; bool-expression; expression) statement;
	swich(bool-expression){
		case expr: // if expression is true all cases below will be executed, use break to avoid it
			statement;
			break;
		...
		default:
			statement;
	}
```

```cpp
	1. break;
	2. continue;
	3. return;
	4. goto: label;
	   label : ++x;
```
